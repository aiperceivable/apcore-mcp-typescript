node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# Editor / IDE / tooling state — keep out of the repo for cross-author parity.
.claude/
.idea/
.vscode/

# Stale leftover from Python tooling — this project uses Vitest, not pytest.
.pytest_cache/

# This project uses pnpm — npm-style lockfiles are not authoritative
package-lock.json
yarn.lock
